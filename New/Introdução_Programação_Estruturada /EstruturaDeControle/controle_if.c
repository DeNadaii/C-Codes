#include <stdio.h>
#include <stdbool.h>

int main(){

    int i = true;
    //quando se tem apenas uma linha não é nescessario as chaves ( {} )
    if(i != true)
        printf("false\n");
    if(i == true)
        printf("true\n");
}
