// 3. Fazer um programa em "C" que lê o preço de um produto e inflaciona esse
// preço em 10% se ele for menor que 100 e em 20% se ele for maior ou igual a
// 100.
// OBS: não use o comando "if" ou o operador de condição "?".
#include <stdio.h>

int main()
{

    // int preco;

    // printf("digite um preco\n");
    // scanf("%d", &preco);

    // if (preco < 100)
    // {
    //     printf("preco inflacionado 10 porcento: %d\n", preco += preco * 0.10);
    // }
    // else
    // {
    //     printf("preco inflacionado 20 porcento: %d\n", preco += preco * 0.20);
    // }

    // return 0;
}